import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: '#FE8F8F'
        },
        common: {
            black: '#000000',
            white: '#FFFFFF',
        },
    },
    shape: {
        borderRadius: 10
    }
})